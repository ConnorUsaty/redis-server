

int main() {
    // get socket (allocates a socket and gives us an fd)
    // bind socket to a port
    // set port to be listening (server), rather then connecting (client)

    // while(1){
        // accept incoming connections and get connection fd
        // error handling
        // send thread to deal with connection fd if valid
    // }

    // close server fd (server socket)

    return 0;
}